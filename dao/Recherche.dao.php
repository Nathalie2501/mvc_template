<?php 
require('modele/Recherche.class.php');

class DaoRecherche  {

	/*Public function read($id) { 

$donnees = DB::select('SELECT 
	tutos.nom AS nomTutos,
	 tutos.description AS descriptionTutos, 
	 MATCH(tutos.nom, tutos.description) AGAINST 
FROM tutos WHERE MATCH (nom) AGAINST ('visage' WHITH QUERY EXPANSION)'array($id));*/
}